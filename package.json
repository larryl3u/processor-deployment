{
  "name": "movement-indexer",
  "version": "1.0.0",
  "main": "dist/main.js",
  "types": "dist/types/main.d.ts",
  "license": "MPL-2.0",
  "private": true,
  "scripts": {
    "help": "npx ts-node main.ts --help",
    "clean": "rm -rf dist cdktf.out .gen",
    "get": "cdktf get",
    "build": "pnpm run clean && pnpm run get && pnpm run compile",
    "compile": "tsc --build",
    "watch": "tsc --build -w",

    "synth": "pnpm run build && cdktf synth",

    "deploy:testnet": "pnpm run synth && cdktf deploy indexer_testnet",
    "deploy:devnet":  "pnpm run synth && cdktf deploy indexer_devnet",
    "deploy:mainnet": "pnpm run synth && cdktf deploy indexer_mainnet",

    "diff:testnet": "pnpm run synth && cdktf diff indexer_testnet",
    "diff:devnet":  "pnpm run synth && cdktf diff indexer_devnet",
    "diff:mainnet": "pnpm run synth && cdktf diff indexer_mainnet",

    "destroy:testnet": "cdktf destroy indexer_testnet",
    "destroy:devnet":  "cdktf destroy indexer_devnet",
    "destroy:mainnet": "cdktf destroy indexer_mainnet"
  },
  "dependencies": {
    "cdktf": "^0.21.0",
    "constructs": "^10.4.2",
    "@cdktf/provider-kubernetes": "^12.1.0",
    "yargs": "^18.0.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/yargs": "^17.0.33",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}